import Navigation from "../components/navigation";

export const metadata = {
  title: "not found",
};

export default function NotFound() {
  return (
    <div>
      <h1>NotFound</h1>
    </div>
  );
}
